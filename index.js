import { registerRootComponent } from 'expo';
import App from './App';

/**
 * Renders the root component to be displayed on different platforms (iOS, Android, Web, tvOS)
 */
registerRootComponent(App);
